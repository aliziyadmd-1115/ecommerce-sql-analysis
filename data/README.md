# Data

The raw data used in this project comes from the Brazilian E-Commerce Public Dataset by Olist.

The original CSV files are not stored in this repository due to file size and repository organization considerations.

To reproduce the project, download the Olist dataset from Kaggle and place the nine source CSV files inside:

`data/raw/`

The SQL scripts in the `sql/` folder can then be executed in numerical order.